# Source corpus — large / rights-bound, not redistributed via this repo
data/pdf/
data/html/

# Generated / derived artifacts (rebuild with process/ scripts)
data/gotw.sqlite
data/gotw_seg.sqlite
data/aat_index.pkl
data/maps/
docs/places.geojsonl
docs/places.geojson
# Large generated site assets — served via the 'site-assets' release + Pages workflow, kept out of git
# history. (Untracked at cutover: git rm --cached -r docs/reader docs/search/gotw-fts.sqlite.png)
docs/reader/
docs/search/gotw-fts.sqlite.png
docs/search/symphonym*.onnx
docs/search/symphonym-embeddings.*
docs/search/symphonym-meta.json
docs/search/symphonym-manifest.json

# Scratch operational helpers (cluster-specific paths; reference rights-bound transcripts)
process/_*.sh

# Secrets — never commit
.env
.env.*

# Python
__pycache__/
*.pyc

# IDE
.idea/
data/jpeg/
data/txt/
docs/plates/

# derived / local (not source)
txt/
.claude/
data/*.sqlite

# served from the site-assets release (42MB), not git — see process/publish_assets.sh
docs/geometry.pmtiles
